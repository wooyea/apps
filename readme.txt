nameing convension:

app dir name: [DISABLED-]app-{appname}-{version}|{-{id}}

service-{appname}.sh


Customer settings :

egrep 'A_CHANGE | A_CHECK' * -r

A_CHANGE: must be changed to right value.
A_CHECK: check is value available. 

